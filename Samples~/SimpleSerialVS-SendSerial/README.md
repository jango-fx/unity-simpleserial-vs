# VS Example: Send Serial

simple script sending a single undulating integer value to serial

- configure the serial port in the `SerialConnection` asset
- add the `SimpleSerialVS - Send` prefab to the scene
- hit **Play**